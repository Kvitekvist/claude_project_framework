@echo off
REM Setup script - prepares a clean machine for development.
REM Customize this for the project's chosen language/framework (see .claude\project_config.md).
REM Examples: create a virtual environment, install dependencies, restore packages.

echo Setup script not yet configured for this project.
echo Edit scripts\setup.bat to add the environment setup steps for this project's stack
echo (e.g. "pip install -r requirements.txt", "npm install", "dotnet restore").
